# FAB Assistant 사용자 화면 개선

## 작업 범위와 종료

- 브랜치: `fix/frontend_adv`, 작업 폴더: `/private/tmp/fab-frontend-adv`
- 원본 `fix/adv_t2s`의 미커밋 작업은 보존. 이 작업은 React 프런트엔드와 문서만 변경한다.
- 사용자가 지정한 종료: **2026-09-08 01:00 KST** (2026-09-07 16:00 UTC).
- 사용량 절약: 레퍼런스 재검색, 서브에이전트, 반복 백엔드 LLM 호출 금지. 의미 있는 프런트엔드 검사만 수행.
- 후속 예약: `fab-1`, 20분 단위, 종료 시간 이후 추가 구현 금지.

## 실행 / 비교

```sh
cd /private/tmp/fab-frontend-adv/apps/web
npm run dev -- --port 5175
npm test
npm run build
```

- 사용자 화면: http://127.0.0.1:5175/
- 차트 예시: http://127.0.0.1:5175/?preview=trend
- 조건 확인: http://127.0.0.1:5175/?preview=clarification
- 데이터 없음: http://127.0.0.1:5175/?preview=unavailable
- 오류: http://127.0.0.1:5175/?preview=error
- 이전 React 평가 대시보드: http://127.0.0.1:5175/trace

Preview는 synthetic 디자인 검토 데이터이며 배너로 표시한다. 실제 채팅/피드백 API를 호출하지 않는다.
개발 서버는 Vite proxy로 기존 localhost:8000 API를 연결한다. `FAB_API_TARGET`으로 proxy 대상 변경 가능.
`VITE_API_BASE`를 사용하면 직접 API URL을 지정한다. 운영 배포는 `/api`와 `/health` reverse proxy 또는 이 환경 변수가 필요하다.
기존 Streamlit `apps/web/app.py`는 변경하지 않았다. 사용자가 첨부한 화면은 Streamlit이며, React 사용자 화면으로의 전환을 제안하는 실험이다.

## 레퍼런스와 적용 판단

2026-09-07에 아래 페이지를 실제 브라우저로 확인했다.

1. [토스 고객센터](https://support.toss.im/): 한 가지 질문과 입력창을 중심으로 한 시작 화면, 간단한 시작점, 넓은 흰 여백.
2. [토스 광고 가이드](https://toss-ads.gitbook.io/guide): 회색 탐색 영역과 흰 본문을 명확하게 분리. 단, GitBook의 디자인도 섞여 있으므로 전체를 토스 고유 시스템으로 해석하지 않는다.
3. [토스 디자인 블로그](https://toss.tech/category/design): 큰 제목, 차분한 보조 텍스트, 경계가 명확한 콘텐츠 묶음.
4. [토스 고객센터 AI 챗봇 사례](https://toss.tech/article/chatbot): 내부 카테고리 탐색보다 자연어 요청을 해결 행동으로 연결하는 원칙. 레퍼런스의 자산이나 로고는 복제하지 않았다.

FAB 적용: 사용자는 답변과 다음 행동을 먼저 본다. 실행 기록은 답변 아래 버튼으로, 에이전트별 반환 데이터는 상세 패널 내 두 번째 펼침으로 제공한다. SQL/계획 JSON을 메인 화면에 기본 노출하지 않는다. 근거와 한계는 숨기지 않고 답변 및 상세에서 확인한다. 연결 상태는 실제 health 요청으로 확인하며 임의로 성공 표시하지 않는다.

## 구현

- 회색 사이드바, 검색 가능한 세션 대화 기록, 신규 대화, 고정 하단 입력창.
- 현황/원인/추이/지식 질문 시작점. 클릭 시 편집 가능한 초안에 입력한다.
- 분석 범위: FAB/라인/공정 drawer. 명시 값의 우선순위와 대화 기억에 대한 안내.
- 결과 우선 답변, 안전한 텍스트 렌더링, 차트/표 전환, CSV 다운로드, 답변 복사, 피드백.
- 에이전트별 접힌 트레이스, 근거 탭, 데이터/SQL 탭, 원본 응답 상세.
- 오류, 중단, 조건 확인, 데이터 없음 상태. 실패 상태를 성공으로 바꾸지 않는다.
- 한글 조합 입력, Shift+Enter, Ctrl/Cmd+K, native dialog 포커스 제어, 방향키 탭 전환, 모바일 숨긴 사이드바 inert.
- SSE 청크/UTF-8/CRLF/끝 버퍼 처리. 중간 끊김을 성공으로 간주하지 않음. 중단/3분 timeout과 요청 중 중복 전송 방지.
- 기존 chart 렌더러 추출. 다중 시리즈 범례에 색상 표시. 미지원 형식/잘못된 값 안내.
- `/trace`로 기존 평가 화면을 보존. 사용자 화면에 평가 suite를 노출하지 않는다.

## 검증 기록 — 첫 구현

- `npm run build` 통과.
- `npm test` 9개 통과: SSE 한글 1바이트 단위 분할, CRLF, 다중 data, 마지막 구분자 누락, 미완료 끊김, 실패/취소, HTTP/잘못된 JSON payload, context payload, CSV escaping, 상태 표시.
- 브라우저 데스크톱 1270×720: 빈 상태, 범위 패널/저장, 실제 질문 발송, 최종 답변, trace count 확인.
- 실제 요청 1회: `fab10 Dry_Etch toolgroup 목록 보여줘`. 기존 API에서 답변, SQL 결과, 근거 2개, 에이전트 기록 수신. **백엔드 최종 status는 failed**였으므로 UI에서도 실패 상태로 표시. 백엔드 답변 품질 성공을 주장하지 않는다.
- 브라우저 390×844: 차트 예시, 상세 패널, text2sql 펼침, 데이터 탭 8행 + SQL 확인. 초기 화면이 답변 하단으로 이동하던 현상을 수정.
- 에이전트 상세와 raw 데이터는 기본 접힘.

## 마감 검증 상태

- 완료: 결과/근거/트레이스 구조, 모바일/키보드, 표/번호 목록, retry payload/피드백 복원, 차트 레이블 helper 검사.
- 브라우저 완료: 실제 API 1회, 조건 확인/데이터 없음/오류, 차트 2계열↔표 8행, 검색 없음, 대화 간 초안 복귀, 새로고침 대화 목록 2개 복원.
- 코드/문서 완료: README 실행 안내, 별도 브랜치 커밋, build 및 17개 검사.
- 미검증: 실제 피드백 저장/CSV 다운로드, 실서버 실패-재시도·수신중단·후속질문 전체 E2E, 긴 표 pagination 및 촘촘한 차트 브라우저 시각 검증.
- 마감까지 범위를 넓히지 않는다. 최종 상태/예약 정지와 결과 보고만 남는다.

## 알려진 제한

- 별도 React 실험이며 기존 Streamlit 사용자 세션은 마이그레이션하지 않는다.
- 대화는 현재 탭의 sessionStorage로 보관한다. 서버 대화 목록 API가 없어서 여러 기기 동기화는 없다.
- Google Fonts Noto Sans KR 사용, 네트워크 미연결 시 시스템 한국어 글꼴 fallback.
- backend 결과 품질, 영어 limitation 중복, 원천 데이터 정확성은 프런트엔드 작업 범위 밖이다.
- 실제 API feedback 저장/CSV 브라우저 다운로드는 아직 E2E 검증 전.
- `/trace`는 기존 코드라 이 실험의 사용자 UX 개선이 적용되지 않는다.

## 후속 점검 — 23:24 KST

- Markdown 표/열 정렬/번호 목록을 지원. 잘못된 표 행은 원문으로 보존하며 HTML은 실행하지 않는다.
- 작은 보조 설명과 주의점의 색상 대비 및 글자 크기를 보완.
- parser 회귀 검사 4개 추가. 전체 13개 테스트 및 production build 통과. 추가 백엔드 요청 없음.

## 후속 점검 — 23:44 KST

- 390×844에서 조건 확인/데이터 없음/오류 화면 확인. 입력창 경계는 y=659~844로 화면 내부 유지.
- 모바일 메뉴 Escape 시 열기 버튼으로 포커스 복귀. inspector ArrowRight로 근거 탭 선택, Escape 후 원래 트레이스 버튼 복귀 확인.
- 빈 근거에 대한 `근거 0` 버튼 제거. 실행 중 상세 상태는 검토 필요 대신 분석 중으로 표시.
- 추가 백엔드 요청 없이 preview로 검증. 기본 viewport 복원.

## 후속 점검 — 00:02 KST

- 재시도가 현재 설정의 FAB로 바뀌는 문제를 수정. 원래 요청 payload를 메시지에 보관해 같은 범위/대화 ID로 재시도한다.
- 재시도 시 입력 중인 다음 질문 초안을 지우지 않는다. 새 대화 생성 시 검색 필터를 초기화한다.
- 새로고침 당시 진행 중이던 피드백은 다시 시도할 수 있도록 pending 해제. 중단된 stream은 수신 중단 상태 복원.
- 위 회귀 검사 2개 추가, 전체 15개와 build 통과. 실제 API 추가 호출 없음.
- README에 사용자 화면/preview/기존 평가 화면과 실행·배포 조건 문서화.
- 재시도/복원은 순수 함수 수준 검증. 브라우저 실제 실패-재시도 및 피드백 저장 E2E는 아직 미검증.

## 후속 점검 — 00:23 KST

- 촘촘한 날짜 축은 최대 6개 레이블로 줄이고 양 끝 날짜는 유지. 점/행 데이터는 삭제하지 않는다. 긴 라벨은 축에서 줄이고 title/원본 표에 전체 값 보존.
- 그룹 막대의 누락 조합을 임의의 0으로 그리지 않도록 변경. 잘못된 null row를 차트 안내로 처리.
- 명시 y축 범위가 0을 포함하지 않을 때 축 선이 차트 밖으로 나가는 문제 보완.
- 축 레이블 회귀 2개 추가. 17개 검사 및 build 통과. 다음 최종 점검에서 브라우저 차트 확인 필요.

## 후속 점검 — 00:46 KST

- 차트↔표에서 2계열/8행 유지, 대화 검색 없는 결과 확인.
- 대화 전환 시 입력 초안 유실 수정. 이전 대화 복귀 후 초안 복원 브라우저 확인. 초안은 메모리 보관이므로 새로고침에는 유지되지 않는다.
- 새로고침 후 저장된 대화 목록 2개 복원 확인. 검토용 탭의 빈 대화이며 추가 API 요청 없음.
- 새 build 및 git diff --check 통과. dependencies symlink와 dist를 web 로컬 gitignore에서 제외.
- 전체 소스 formatter 작업은 기능 검증 우선으로 보류. 기존 Streamlit/백엔드는 변경하지 않음.

## 추가 개선 — 2026-09-08 06:20~07:50 KST

사용자가 1시간 30분을 추가 승인. 이전 01:00 마감은 이번 추가 작업에 적용되지 않는다.
예약 `fab-1` 재개, 새 마감 07:50 KST (2026-09-07 22:50 UTC). 원본 작업 폴더 보존/사용량 절약 기조 유지.

- 첫 수정: 긴 표 검색, 숫자/문자 열 정렬, 페이지 탐색, 검색 결과 없음 상태. null을 0으로 정렬하지 않는다.
- 20행 이하에서는 검색 도구를 감춰 작은 결과에 불필요한 UI가 늘지 않게 한다.
- `?preview=large`에 65행 예시와 촘촘한 날짜 축 검토 화면 추가.
- 순수 함수 테스트 3개 추가: 숫자/빈 값/한글/중첩값/원본 보존. 전체 20개 통과, build 통과.

### 추가 작업 다음 순서

1. 긴 표/정렬/검색/페이지/CSV를 브라우저에서 검증.
2. 새로고침 후 활성 대화/초안 복원, 답변 저장과 트레이스 요약을 검토/구현.
3. 제한된 mock으로 실패/취소/재시도/피드백 오류 흐름 검증. 실서버 LLM 대량 호출 금지.
4. 변경된 기능만 최종 검사/문서화/커밋. 07:50 이후 추가 개발 금지, 예약 PAUSED와 결과 보고.

- 브라우저 검증: 첫 페이지 20행, 다음 페이지 시작 2020-01-21, 제품 2 검색 22/65행, count 오름차순 첫 값 108 확인.
- CSV 버튼 클릭 후 IAB download event 10초 내 미수신. 다운로드 성공 주장하지 않음. 후속 점검에서 저장 방식/브라우저 호환성 확인 필요.
- 필터 상태와 저장 범위가 혼동되지 않도록 버튼을 ‘전체 데이터 CSV 저장’으로 명시.
- 아침에 dev 서버가 종료되어 있어 5175에서 재시작했음.
