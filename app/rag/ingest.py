from pathlib import Path


def ingest_documents(input_dir: Path, collection: str) -> int:
    """TODO: PDF/DOCX 추출, chunking, embedding, vector DB 적재를 구현합니다."""
    _ = (input_dir, collection)
    return 0

